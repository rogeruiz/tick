[package]
name = "tick"
version = "0.2.0"
authors = ["Roger Steve Ruiz <hi@rog.gr"]
description = "A time-tracking cli tool that generates useful reports for the last week."
repository = "https://github.com/rogeruiz/tick"
readme = "README.md"
keywords = [
  "cli",
  "timekeeping",
  "time-tracking"
]
license = "MIT"

[dependencies]
dotenv = "0.10.0"
chrono = "0.3.1"

[dependencies.clap]
version = "2.24.2"
features = [
  "suggestions",
  "color",
  "wrap_help",
  "yaml",
]

[dependencies.diesel]
version = "0.13.0"
features = [ "sqlite" ]

[dependencies.diesel_codegen]
version = "0.13.0"
features = [ "sqlite" ]
